import PageNotFound from "../components/PageNotFound";
import { RouteComponentProps } from "@reach/router";

function PageNotFoundView(props: RouteComponentProps) {
  return <PageNotFound />;
}

export default PageNotFoundView;
