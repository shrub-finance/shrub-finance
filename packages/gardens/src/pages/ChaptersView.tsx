import Chapters from "../components/Chapters";
import { RouteComponentProps } from "@reach/router";

function ChaptersView(props: RouteComponentProps) {
  return <Chapters />;
}

export default ChaptersView;
